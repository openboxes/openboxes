import React from 'react';
import Modal from 'react-modal';

const PreferenceTypeModal = ({ isOpen }) => <Modal isOpen={isOpen} className="modal-content">
  <div>
    preference type modal
  </div>
</Modal>;

export default PreferenceTypeModal;
