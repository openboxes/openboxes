// This file contains constants for different types of modals that include tables.
// We need these to distinguish between multiple table modals within the same file.
const modalWithTableType = {
  EXPIRATION: 'expiration',
  DUPLICATES: 'duplicates',
};

export default modalWithTableType;
