version: "3.8"
services:
    app:
      extends: 
        file: docker-compose-base.yml
        service: app

    nginx:
      extends: 
        file: docker-compose-base.yml
        service: nginx
      depends_on:
          - app
