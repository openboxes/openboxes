package org.pih.warehouse


/**
 * A supplier has a product list.  The product list contains all products that
 * the supplier stocks.  This does not encapsulate the current stock -- that
 * is handled by the Inventory.
 */
class ProductList {

    static constraints = {
    }
}
