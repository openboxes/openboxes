package org.pih.warehouse

/**
 * A location can be a warehouse and a supplier.  We want to keep warehouse
 *
 * Not sure if this makes sense.
 *
 */
class Location {

    String name
    Address address 


    static constraints = {
    }
}
