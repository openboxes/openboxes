package org.pih.warehouse.api

import org.pih.warehouse.core.BaseController

/**
 * Base class for all API Controller components.
 */
abstract class BaseApiController extends BaseController {
    // Currently does nothing. Created as a safety measure in case we need to add some common behaviour in the future.
}
